第三周自测题解答
1. 推导题：VAE 的 ELBO 公式与重参数化技巧
ELBO 公式：

$$\log p(x) \geq \mathbb{E}{q\phi(z|x)}[\log p_\theta(x|z)] - D_{KL}(q_\phi(z|x) | p(z))$$

各项含义：

$\log p(x)$：数据的对数似然（我们想最大化的目标，但直接计算 intractable）
$\mathbb{E}{q\phi(z|x)}[\log p_\theta(x|z)]$：重构项。从编码器采样 z，再用解码器重构 x 的期望对数似然。鼓励模型准确重构输入。
$D_{KL}(q_\phi(z|x) | p(z))$：正则项。编码器后验 $q_\phi(z|x)$ 与先验 $p(z)$（通常为 $\mathcal{N}(0, I)$）之间的 KL 散度。鼓励潜空间结构化、平滑。
推导过程：

$$\log p(x) = \log \int p_\theta(x|z)p(z)dz$$

引入变分后验 $q_\phi(z|x)$：

$$\log p(x) = \log \int \frac{q_\phi(z|x)}{q_\phi(z|x)} p_\theta(x|z)p(z)dz$$

由 Jensen 不等式（$\log$ 是凹函数）：

$$\geq \int q_\phi(z|x) \log \frac{p_\theta(x|z)p(z)}{q_\phi(z|x)} dz = \mathbb{E}q[\log p\theta(x|z)] - D_{KL}(q | p(z))$$

重参数化技巧：

问题：从 $q_\phi(z|x) = \mathcal{N}(\mu_\phi(x), \sigma^2_\phi(x))$ 采样不可微（采样操作阻断梯度回传）。

解法：将随机性外移：
$$z = \mu_\phi(x) + \sigma_\phi(x) \odot \epsilon, \quad \epsilon \sim \mathcal{N}(0, I)$$

这样 $z$ 对 $\phi$ 可微，梯度可以通过 $\mu$ 和 $\sigma$ 回传到编码器。

2. 架构题：Neural Turing Machine

```
┌─────────────────────────────────────────────┐
│                  Controller                  │
│            (LSTM / Feedforward)              │
│                                             │
│   输入: x_t ──►  隐藏状态 h_t  ──► 输出: y_t │
│                    │    │                    │
│              ┌─────┘    └─────┐              │
│              ▼                ▼              │
│         Read Head(s)    Write Head(s)        │
│          │                │                  │
│          │  w_t^r         │  w_t^w           │
│          │ (读权重)        │ (写权重)          │
│          │                │                  │
│          │    ┌───────────┘                  │
│          ▼    ▼                              │
└──────────┼────┼──────────────────────────────┘
           │    │
     ┌─────▼────▼─────────────────┐
     │    Memory Matrix M_t       │
     │    (N slots × W dims)      │
     │                            │
     │  读: r_t = Σ w_t^r(i)·M(i)│
     │                            │
     │  写: M̃ = M·(1 - w_t^w·e^T)│  ← 擦除 (erase vector e)
     │  M_t = M̃ + w_t^w · a^T    │  ← 写入 (add vector a)
     └────────────────────────────┘
```

寻址机制 (生成权重 w_t):
  1. Content-based: cosine similarity(k_t, M(i))
  2. Location-based: interpolation → shift → sharpen
关键交互：

Controller 输出寻址参数（key, shift, gate 等）给读写头
读头通过注意力权重从 Memory 中加权读取，结果反馈给 Controller
写头通过 erase + add 两步更新 Memory
所有操作连续可微，可端到端训练
3. 对比题：记忆维度对比
维度	LSTM	NTM	Relational RNN	Transformer
记忆容量	固定（隐藏状态维度）	可扩展（外部矩阵 N×W）	固定但更丰富（多记忆槽+关系推理）	与序列长度线性增长（KV cache）
访问方式	隐式门控（forget/input gate）	显式寻址（content + location）	多头注意力在记忆槽间推理	全局 self-attention
写入方式	门控混合（覆盖式）	擦除+写入（精确编辑）	基于注意力的记忆更新	无显式写入（前向传播中隐式）
可扩展性	差（增大隐藏层 = 参数爆炸）	好（增加 memory slots 不影响控制器）	中等（槽数固定，关系计算 O(n²)）	好（但 attention O(n²) 限制长度）
长距离依赖	中等（梯度消失缓解但未消除）	强（外部存储无梯度衰减）	强（关系推理增强记忆利用）	强（直接全局连接）
结构化存储	无（扁平向量）	有（矩阵，可寻址）	有（多槽，关系图）	隐式（通过 attention pattern）
核心洞察：从 LSTM → NTM → Transformer，记忆从"隐式嵌入参数中"演变为"显式可寻址的外部结构"，访问方式从"局部门控"演变为"全局注意力"。

4. 分析题：MDL 与奥卡姆剃刀
**最小描述长度（MDL）**原理：

$$MDL(x, M) = L(M) + L(x|M)$$

$L(M)$：描述模型本身所需的比特数（模型复杂度）
$L(x|M)$：用模型编码数据所需的比特数（数据拟合程度）
为什么选择 MDL 更低的模型？

MDL 更低意味着"模型 + 用模型压缩后的数据"总共需要更少的信息量。这等价于找到了数据中更好的规律性（regularity）——用更少的比特就能完整描述数据。

与奥卡姆剃刀的关系：

奥卡姆剃刀说："如无必要，勿增实体"——在同等解释力下，选择最简单的理论。

MDL 是奥卡姆剃刀的数学形式化：

过于简单的模型：$L(M)$ 小，但 $L(x|M)$ 大（欠拟合，压缩不好）
过于复杂的模型：$L(x|M)$ 小，但 $L(M)$ 大（过拟合，模型本身需要大量描述）
MDL 最优模型：在复杂度和拟合之间取得最佳平衡
这也直接联系到 Kolmogorov 复杂度和 Solomonoff 归纳推理——真正的智能在于找到数据的最短描述（最优压缩）。

5. 综合题：三篇论文的技术传承
选择：LSTM → Sequence to Sequence → Attention (Transformer)

1. LSTM (Hochreiter & Schmidhuber, 1997)
解决了 vanilla RNN 梯度消失问题，通过门控机制（forget, input, output gate）实现长距离信息的选择性记忆与遗忘。它是序列建模的基石，证明了"学会何时记忆、何时遗忘"比"记住一切"更重要。

2. Sequence to Sequence (Sutskever et al., 2014)
将两个 LSTM 组合为 encoder-decoder 架构，证明了固定维度的向量可以编码变长序列的语义。这一工作直接催生了神经机器翻译的实用化，但也暴露了瓶颈：所有信息被压缩到单一向量中，长序列信息丢失严重。

3. Attention / Transformer (Vaswani et al., 2017)
正是为了解决 Seq2Seq 的信息瓶颈，注意力机制允许解码器直接"看到"编码器的所有位置。Transformer 更进一步，完全去除递归，用 self-attention 实现全局并行计算。这不仅解决了长距离依赖，更释放了大规模并行训练的潜力，直接催生了 GPT/BERT 等大模型时代。

传承链：LSTM 解决了"能否记住" → Seq2Seq 解决了"能否转换" → Transformer 解决了"能否高效地全局关联"。每一步都是对前一步瓶颈的突破。